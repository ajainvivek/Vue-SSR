{
	"scripts": {
		"clean": "lerna clean",
		"bootstrap": "npm i && lerna bootstrap",
		"publish": "lerna publish --conventional-commits --changelog-preset angular-bitbucket --skip-npm"
	},
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
